MongoDb installation

Create a folder for database 

Data->db

Go to mondodb bin folder use cmd

mongod.exe --dbpath "C:\Data"


localhost:27017


127.0.0.1:27017/

Creating database will be created only after creating collections

MongoDB waits until you have created a collection (table), 
with at least one document (record) before it actually creates the database (and collection).